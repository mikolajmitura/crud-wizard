package pl.jalokim.crudwizard.genericapp.validation.generic;

import org.springframework.stereotype.Component;
import pl.jalokim.crudwizard.core.datastorage.RawEntityObject;
import pl.jalokim.crudwizard.core.metamodels.ClassMetaModel;

@Component
public class GenericValidator {

    // TODO #02 invoke validation on fields and objects translated object
    public void validate(RawEntityObject rawEntityObject, ClassMetaModel classMetaModel) {

    }
}
