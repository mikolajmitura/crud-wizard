    private ${methodReturnType} ${methodName}(GenericMapperArgument genericMapperArgument, ${sourceObjectType} sourceObject) {
        switch(sourceObject) {
${cases}
          default: ${whenNotMappedEnum};
        }
    }
